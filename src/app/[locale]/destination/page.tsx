import { Main } from '@/components/Destination';

import React from 'react';

export default function page() {
  return (
    <section className="container flex items-center justify-center min-h-[50svh] h-full">
      <Main />
    </section>
  );
}
